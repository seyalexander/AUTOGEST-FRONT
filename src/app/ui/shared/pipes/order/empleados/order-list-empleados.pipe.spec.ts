import { OrderListEmpleadosPipe } from './order-list-empleados.pipe';

describe('OrderListEmpleadosPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderListEmpleadosPipe();
    expect(pipe).toBeTruthy();
  });
});
