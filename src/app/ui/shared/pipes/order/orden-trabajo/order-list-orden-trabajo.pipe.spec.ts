import { OrderListOrdenTrabajoPipe } from './order-list-orden-trabajo.pipe';

describe('OrderListOrdenTrabajoPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderListOrdenTrabajoPipe();
    expect(pipe).toBeTruthy();
  });
});
