import { OrderListTipoDocumentoPipe } from './order-list-tipo-documento.pipe';

describe('OrderListTipoDocumentoPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderListTipoDocumentoPipe();
    expect(pipe).toBeTruthy();
  });
});
