import { OrderListAutosPipe } from './order-list-autos.pipe';

describe('OrderListAutosPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderListAutosPipe();
    expect(pipe).toBeTruthy();
  });
});
