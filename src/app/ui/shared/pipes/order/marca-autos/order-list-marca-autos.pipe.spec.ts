import { OrderListMarcaAutosPipe } from './order-list-marca-autos.pipe';

describe('OrderListMarcaAutosPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderListMarcaAutosPipe();
    expect(pipe).toBeTruthy();
  });
});
