import { OrderListClientesPipe } from './order-list-clientes.pipe';

describe('OrderListClientesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderListClientesPipe();
    expect(pipe).toBeTruthy();
  });
});
