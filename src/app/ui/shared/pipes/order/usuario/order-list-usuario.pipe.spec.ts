import { OrderListUsuarioPipe } from './order-list-usuario.pipe';

describe('OrderListUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderListUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
