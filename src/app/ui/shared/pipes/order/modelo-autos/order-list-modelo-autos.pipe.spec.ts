import { OrderListModeloAutosPipe } from './order-list-modelo-autos.pipe';

describe('OrderListModeloAutosPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderListModeloAutosPipe();
    expect(pipe).toBeTruthy();
  });
});
