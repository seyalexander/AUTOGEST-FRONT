import { OrderListRolUsuarioPipe } from './order-list-rol-usuario.pipe';

describe('OrderListRolUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderListRolUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
